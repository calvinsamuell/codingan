const basic ={
    mounted(){
        console.log('ini basic mixins');
    },
    methods: {
        onPress(){
            console.log('ini dari onpress mixins')
        }
    }
}

export default basic;